'use strict';

/**
 * egg-useragent default config
 * @member Config#useragent
 * @property {Boolean} [enable=true] - Enable middleware, default true.
 */
exports.useragent = {
  // 是否启用
  enable: true,
};
